import React from "react";

const RightPanel = ({ children }) => {
    return (
        <div className="right-panel bg-section-bg h-full flex flex-col pt-4">
        </div>
    );
};

export default RightPanel;