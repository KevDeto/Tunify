import React from "react";

const CenterPanel = ({ children }) => {
    return (
        <div className="center-panel bg-section-bg h-full flex flex-col pt-4 overflow-y-auto">
            
        </div>
    );
};

export default CenterPanel;